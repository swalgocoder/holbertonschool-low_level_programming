#include <stdio.h>
#include <stdlib.h>
/**
 * main - programm starts
 * evaluates and prints letters,lowercase  putchar function
 * Return: 0
 */
	int main(void)
	{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	putchar(i);

	putchar('\n');

	return (0);
	}
