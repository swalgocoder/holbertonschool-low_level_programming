#include <stdio.h>
/**
 *main - program starts, evaluates and prints
 *numbers (base 16)
 *Return: 0
 */
	int main(void)
	{
	int n = 48;

	while (n < 58)
	{
	putchar(n);
	n++;
	}
	n = 'a';
	while (n <= 'f')
	{
	putchar(n);
	n++;
	}
	putchar("\n");

	return (0);
	}
