#include <stdio.h>
/**
 *main - program starts, evaluates and prints 
 *numbers (base 10) 0 to 9
 *Return: 0
 */
int main(void)
{
	int n = 0;

	while (n < 10)
	{
	printf("%d", n);
	n++;
	}
	printf("\n");

	return (0);
}
