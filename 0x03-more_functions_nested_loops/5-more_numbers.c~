#include "holberton.h"

/**
 * more_numbers - print many numbers
 *
 * Return: nothing
 */
void more_numbers(void)
{
	int a;
	int i;

	for (i = 0; i <= 9; i++)
	{
		for (a = 0; a <= 14; a++)
		{
			if (a >= 10)
			{
				_putchar(a / 10 + '0');
			}
			_putchar(a % 10 + '0');
		}
		_putchar('\n');
		a = 0;
	}
}
