#include "holberton.h"
/**
 *_isupper - checks if character is lowercase
 *@c: -  The character to check
 *Return: 1 if c is uppcase, 0 ohterwise
 */

	int _isupper(int c)
	{
	
		if (c >= 'A' && c <= 'Z')
		{
		return (1);
		}
		
		return (0);

	}
