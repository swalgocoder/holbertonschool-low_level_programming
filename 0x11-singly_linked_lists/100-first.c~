#include <stdio.h>
/**
 * prefix - prints text before main func
 *
 * Return: returns none.
 */
void prefix(void) __attribute__ ((constructor));

void prefix(void)
{
	printf("You're beat! and yet, you must allow,\n");
	printf("I bore my house upon my back!\n");
}
